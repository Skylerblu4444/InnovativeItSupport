import { test, expect } from '@playwright/test';

test('homepage shows title and client portal link', async ({ page }) => {
  await page.goto('/');
  await expect(page.locator('text=BluHorizon')).toBeVisible();
  await expect(page.locator('text=Client Portal')).toBeVisible();
});
